

spring:
  data:
    redis:
      host: localhost
      port: 6379

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MALL_USER}
    password: ${MALL_PASS}
    properties:
      mail:
      smtp:
      auth: true
      starttls:
      enable: true

